---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mediamega-data
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Ti
  storageClassName: cephfs-hdd
